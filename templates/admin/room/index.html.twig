{% extends 'admin/index.html.twig' %}

{% block title %}Room index{% endblock %}

{% block body %}
    <h1>Room index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Numero</th>
            <th>Type</th>
            <th>Prix</th>
            <th>Capacité</th>
            <th>description</th>
            <th>Image</th>
            <th>Statut</th>
        </tr>
        </thead>
        <tbody>
        {% for room in Chambres %}
            <tr>
                <td>{{ room.id }}</td>
                <td>{{ room.numero }}</td>
                <td>{{ room.type }}</td>
                <td>{{ room.prix }}</td>
                <td>{{ room.capacite }}</td>
                <td>{{ room.description }}</td>
                <td>{{ room.image }}</td>
                <td>{{ room.statut }}</td>
                <td>
                    <a href="{{ path('admin_room_show', {'id': room.id}) }}">show</a>
                    <a href="{{ path('admin_room_edit', {'id': room.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_room_new') }}">Create new</a>
{% endblock %}