{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Room</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ room.id }}</td>
        </tr>
        <tr>
            <th>Numero</th>
            <td>{{ room.numero}}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ room.type }}</td>
        </tr>
        <tr>
            <th>Prix</th>
            <td>{{ room.prix }}</td>
        </tr>
        <tr>
            <th>Capacité</th>
            <td>{{ room.capacite }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ room.description }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ room.image }}</td>
        </tr>
        <tr>
            <th>Statut</th>
            <td>{{ room.statut }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_room_index') }}">back to list</a>

    <a href="{{ path('admin_room_edit', {'id': room.id}) }}">edit</a>

    {{ include('admin/room/_delete_form.html.twig') }}
{% endblock %}